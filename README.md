# python-linked_list-beginner-notes
A Linked List is a dynamic data structure where elements, called nodes, are connected using pointers. Unlike arrays, linked lists do not require contiguous memory allocation, making insertions and deletions efficient. They are widely used in data structures, memory management, and algorithm design.
